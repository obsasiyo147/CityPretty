<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cityprettyapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".AddWomanHaircutToCartActivity"></activity>
        <activity android:name=".AddManHaircutToCartActivity" />
        <activity android:name=".AddManicureToCartActivity" />
        <activity android:name=".AddPedicureToCartActivity" />
        <activity android:name=".BeauticianSide" />
        <activity android:name=".BeauticiansList" />
        <activity android:name=".MakePaymentActivity" />
        <activity android:name=".PayPalActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".AppointmentActivity" />
        <activity android:name=".ChangeSettings" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ServiceList" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>